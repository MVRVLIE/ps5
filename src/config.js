/** Настройки игры. Всё, что имеет смысл менять перед публикацией, собрано здесь. */
export const CONFIG = {
  /** Размер поля и подквадрата. */
  grid: 9,
  box: 3,
  /** Сколько фигур в лотке одновременно. */
  trayCount: 3,

  /** Технические имена из консоли разработчика Яндекс Игр. */
  leaderboardName: 'score',

  /** Экономика очков. */
  score: {
    perCell: 1,          // за каждую клетку поставленной фигуры
    lineBase: 18,        // база за одну очищенную линию/квадрат
    comboStep: 0.25,     // +25% за каждый ход подряд с очисткой
    comboMax: 3,         // потолок множителя
  },

  /** Спасения за rewarded-рекламу в одной партии. */
  maxRescues: 2,

  /** Свой кулдаун межстраничной рекламы (у платформы свой — минута). */
  interstitialCooldownMs: 90_000,
  /** Реклама не показывается, пока сыграно меньше стольких партий. */
  interstitialAfterGames: 1,
  /** Липкий баннер. Включайте, только если он не перекрывает интерфейс. */
  stickyBanner: false,

  /** Предложить ярлык на рабочий стол после N партий. */
  shortcutAfterGames: 2,
  /** Просить оценить игру, начиная с этого счёта. */
  reviewAfterScore: 400,

  /** Ключ локального сохранения. */
  storageKey: 'nine.save.v1',

  /** Палитра блоков (индекс 1..8 в клетках поля). */
  colors: [
    null,
    '#ff6b6b', '#ffa94d', '#ffd43b', '#51cf66',
    '#38d9a9', '#4dabf7', '#b197fc', '#f783ac',
  ],
};

export const GRID = CONFIG.grid;
export const CELLS = CONFIG.grid * CONFIG.grid;
